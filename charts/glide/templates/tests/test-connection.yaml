apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glide.fullname" . }}-test-connection"
  labels:
    {{- include "glide.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glide.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
